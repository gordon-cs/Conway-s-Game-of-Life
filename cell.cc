#include <iostream>
#include "cell.h"

using namespace std;

Cell::Cell(state cellState, int coordinates) { }

state getState() {
    return cellState;
}

int getPosition() {
    return coordinates;
}
    
